type Release = {
    id: string,
    name: string,
    body: string,
    tag: string
}

const Release = (props: Release) => {
    const abc = 123;
    return (
        
    )
}

export default Release;