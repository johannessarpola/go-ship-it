
type Props = {
    name: string
    releases: Release[]
}

type Release = {
    id: string,
    name: string,
    body: string,
    tag: string
}

const Repository = (props: Props) => {
    const abc = 123;
    return ()
}

export default Repository;



